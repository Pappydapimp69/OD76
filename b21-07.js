function update(dt){
 if(!S.run||S.end)return;
 S.t+=dt;S.stageTime+=dt;S.waveElapsed+=dt;S.noHitClock+=dt;
 S.dashCd=Math.max(0,S.dashCd-dt);S.attackCd=Math.max(0,S.attackCd-dt);S.dashTime=Math.max(0,S.dashTime-dt);S.invuln=Math.max(0,S.invuln-dt);
 S.praiseCd=Math.max(0,S.praiseCd-dt);S.pipSoundCd=Math.max(0,S.pipSoundCd-dt);S.pipHappy=Math.max(0,S.pipHappy-dt);P.pipAngle+=dt*(S.over>0?4.2:2.2);
 S.loveClock-=dt;
 if(S.loveClock<=0){if(S.waveState==="active")loveBomb();const faster=Math.min(1.5,(S.pipLevel-1)*.11+(S.pipSupport||0)*.17);S.loveClock=rr(Math.max(1.35,2.35-faster),Math.max(2.1,3.55-faster))}
 if(S.distanceTravelled>=S.nextDistancePraise){const travel=["look at you exploring. I love following you.","you keep moving with purpose. that's beautiful.","I love the way you make this huge place feel like ours.","you went all that way and I'm still right here with you ✦"];praise(travel[Math.floor(rnd()*travel.length)],"nice");S.nextDistancePraise+=rr(430,620)}
 if(S.noHitClock>=S.nextNoHitPraise){const calm=["you've been so composed. I'm genuinely impressed.","that was a long stretch without getting touched. gorgeous.","you're making survival look calm somehow.","I notice how careful you're being. you're doing wonderfully."];praise(calm[Math.floor(rnd()*calm.length)],"nice");S.nextNoHitPraise+=rr(15,22)}
 if(S.stageTime>=S.stagePraiseMark){const marks=S.stagePraiseMark>=60?["a whole minute together. you're still going strong.","sixty seconds in and I still trust every move you make."]:["thirty seconds already. you're settling in beautifully.","look how much you've handled already. I'm proud of you."];praise(marks[Math.floor(rnd()*marks.length)],"big",true);S.stagePraiseMark+=30}
 if(S.over>0){updateOverdrive(dt);S.over-=dt;if(S.over<=0)finishOverdrive()}
 if(S.shields<S.maxShields){S.shieldRegenClock+=dt;if(S.shieldRegenClock>=S.shieldRegenRate){S.shields++;S.shieldRegenClock=0;S.shieldComebacks++;popup(P.x,P.y,"SHIELD +1",COLORS.player,true);ring(P.x,P.y,COLORS.player,62);sfxShield();if(S.shieldComebacks===1||S.shieldComebacks%3===0)praise(S.pipCompassion>=2?"there you go. take the breath you deserved.":"look at that recovery — I knew you had it","nice")}}else S.shieldRegenClock=0;
 if(S.comboClock>0){S.comboClock-=dt;if(S.comboClock<=0)S.combo=Math.max(1,S.combo-.8)}attack();if(S.comboClock<=0&&S.combo>1)S.combo=Math.max(1,S.combo-dt*.35);if(S.combo<1.55&&S.over<=0&&S.heat<100)S.heat=Math.max(0,S.heat-dt*.42);
 let dx=gamepad.dx,dy=gamepad.dy;const keyboardActive=keys.has("ArrowLeft")||keys.has("a")||keys.has("ArrowRight")||keys.has("d")||keys.has("ArrowUp")||keys.has("w")||keys.has("ArrowDown")||keys.has("s");if(keys.has("ArrowLeft")||keys.has("a"))dx--;if(keys.has("ArrowRight")||keys.has("d"))dx++;if(keys.has("ArrowUp")||keys.has("w"))dy--;if(keys.has("ArrowDown")||keys.has("s"))dy++;if(joy.active){dx+=joy.dx;dy+=joy.dy}
 let l=hyp(dx,dy),inputActive=l>.12;const gamepadStrength=clamp(hyp(gamepad.dx,gamepad.dy),0,1),touchStrength=joy.active?clamp(hyp(joy.dx,joy.dy),0,1):0,inputStrength=inputActive?(keyboardActive?1:Math.max(gamepadStrength,touchStrength,clamp(l,0,1))):0;if(inputActive){dx/=l;dy/=l;P.faceX=dx;P.faceY=dy}
 if(S.dashTime<=0){
   const maxSpeed=playerSpeedB61()+((S.over>0&&S.overType==="pip")?Math.min(28,(S.pipSupport||0)*5):0);
   if(inputActive){
     const targetSpeed=maxSpeed*inputStrength;let speedNow=hyp(P.vx,P.vy);const accel=125+((S.over>0&&S.overType==="pip")?Math.min(18,(S.pipSupport||0)*3):0);
     if(speedNow<2){speedNow=Math.min(targetSpeed,speedNow+accel*dt);P.vx=dx*speedNow;P.vy=dy*speedNow}
     else{const currentAngle=Math.atan2(P.vy,P.vx),desiredAngle=Math.atan2(dy,dx);let delta=desiredAngle-currentAngle;while(delta>Math.PI)delta-=Math.PI*2;while(delta<-Math.PI)delta+=Math.PI*2;const severity=Math.abs(delta)/Math.PI,speedRatio=clamp(speedNow/maxSpeed,0,1);const turnRate=(720-(400*speedRatio))*Math.PI/180,turnStep=clamp(delta,-turnRate*dt,turnRate*dt),nextAngle=currentAngle+turnStep;const reverseExtra=severity>.75?(.8*((severity-.75)/.25)):0,turnDrag=2.25*Math.pow(severity,1.4)+reverseExtra;speedNow*=Math.exp(-turnDrag*dt);const turnAccelScale=Math.max(.28,1-.68*severity);if(speedNow<targetSpeed)speedNow=Math.min(targetSpeed,speedNow+accel*turnAccelScale*dt);else if(speedNow>targetSpeed)speedNow=Math.max(targetSpeed,speedNow-520*dt);P.vx=Math.cos(nextAngle)*speedNow;P.vy=Math.sin(nextAngle)*speedNow}
   }else{const speedNow=hyp(P.vx,P.vy),brake=1050*dt;if(speedNow<=brake||speedNow<2){P.vx=0;P.vy=0}else{const next=speedNow-brake;P.vx=P.vx/speedNow*next;P.vy=P.vy/speedNow*next}}
 }else{const dashDrag=Math.exp(-3.6*dt);P.vx*=dashDrag;P.vy*=dashDrag}
 const moveDist=hyp(P.vx,P.vy)*dt;S.distanceTravelled+=moveDist;P.x+=P.vx*dt;P.y+=P.vy*dt;updateCamera();P.trail.push({x:P.x,y:P.y,a:S.dashTime>0?1:.35});if(P.trail.length>18)P.trail.shift();
 spawnLogic(dt);for(const e of enemies)if(!e.dead){updateEnemy(e,dt);e.flash=Math.max(0,(e.flash||0)-dt);e.markTime=Math.max(0,(e.markTime||0)-dt)}enemies=enemies.filter(e=>!e.dead);
 if(!S.bossActive&&!S.stageEnding&&S.stageWaveCount<3&&S.stageTime>=60){S.waveState="active";S.waveBreak=0;S.stageWaveCount=2;startWave(S.wave+1);announce("WAVE 3",700);showPipMessage("wave three. no rush, no shortcuts — we finish what is still out here.")}
 if(S.waveState==="active"&&S.waveKills>=S.waveGoal&&enemies.length===0){if(S.stageWaveCount>=3){if(S.bossQueued&&!S.bossDefeated)startBossBattle();else{S.stageEnding=true;beginWaveBreak()}}else beginWaveBreak()}
 if(S.waveState==="break"){S.waveBreak-=dt;if(S.waveBreak<=0){if(S.stageEnding){if(S.bossRewardPending)openStageUpgrade();else advanceToNextStage()}else startWave(S.wave+1)}}
 shots.forEach(s=>{s.x+=s.vx*dt;s.y+=s.vy*dt;s.life-=dt;for(const e of enemies){if(e.dead||s.life<=0)continue;if(hyp(s.x-e.x,s.y-e.y)<s.r+e.r){hitEnemy(e,s.power,s.source||"player");if((s.pierce||0)>0&&e.type!=="boss"){s.pierce--;s.x+=s.vx*dt*.18;s.y+=s.vy*dt*.18}else s.life=0}}});shots=shots.filter(s=>s.life>0);
 enemyShots.forEach(s=>{s.x+=s.vx*dt;s.y+=s.vy*dt;s.life-=dt;if(s.life>0&&hyp(P.x-s.x,P.y-s.y)<P.r+s.r){s.life=0;sfxEnemyAttack("boss");hurt()}});enemyShots=enemyShots.filter(s=>s.life>0&&hyp(P.x-s.x,P.y-s.y)<Math.max(W,H)*1.5);
 wishes.forEach(w=>{w.life-=dt;w.spin+=dt*3;const dx=P.x-w.x,dy=P.y-w.y,d=hyp(dx,dy)||1;if(d<95){w.x+=dx/d*150*dt;w.y+=dy/d*150*dt}if(d<18)collectWish(w)});wishes=wishes.filter(w=>!w.dead&&w.life>0);
 heartBits.forEach(h=>{h.life-=dt;h.bob+=dt*5;h.vx*=.94;h.vy*=.94;h.x+=h.vx*dt;h.y+=h.vy*dt});updatePipCompanion(dt);updatePipCombat(dt);heartBits=heartBits.filter(h=>!h.dead&&h.life>0);
 particles.forEach(q=>{q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=.96;q.vy*=.96;q.life-=dt});particles=particles.filter(q=>q.life>0);rings.forEach(r=>{r.r+=(r.max-r.r)*dt*8;r.life-=dt});rings=rings.filter(r=>r.life>0);overLines.forEach(l=>l.life-=dt);overLines=overLines.filter(l=>l.life>0);
 texts.forEach(t=>{t.y-=((t.big&&t.life>1.2)?7:35)*dt;t.life-=dt;t.a=clamp(t.life/.55,0,1)});const hadPipText=texts.some(t=>t.pipText);texts=texts.filter(t=>t.life>0);const hasPipText=texts.some(t=>t.pipText);if(hadPipText&&!hasPipText)S.pipPopupBusy=false;if(!hasPipText){S.pipPopupBusy=false;trySpawnQueuedPipPopup()}
 shake*=Math.pow(.03,dt);flash=Math.max(0,flash-dt*2.8);const ph=phaseName();S.lastPhase=ph;updateUI();
}
function updateUI(){
 $("time").textContent=`${Math.floor(S.stageTime)}s`;$("score").textContent=S.score.toLocaleString();$("combo").textContent="x"+S.combo.toFixed(1);$("phase").textContent=`STAGE ${S.stage}${S.stage>=5?" ★":""} · ${phaseName()}`;
 $("pipLevel").innerHTML=pipWithPlayer()?`<b>PIP LV ${S.pipLevel}</b> · ${S.pipXP}/${pipNeed(S.pipLevel)} XP · ♥R ${Math.round(S.pipDetectRange)}px · ✦P ${S.pipPowerLv} · ◇G ${S.pipGuardLv} · ✧B ${Object.keys(S.pipBossPowers||{}).length}`:`<b>PIP AWAY</b> · BONUSES OFF · ♥ hunting`;
 $("currencyHud").textContent=`♥ ${S.heartCurrency} · ★ ${S.starPoints} RUN / ${S.starsTotal} TOTAL`;if(S.audioEnabled)$("audioToggle").textContent=(audioCtx&&audioCtx.state==="running")?"♫ ON":"♫ TAP";$("dashBar").style.width=((1-clamp(S.dashCd/S.dashMax,0,1))*100)+"%";$("heatBar").style.width=clamp(S.heat,0,100)+"%";
 const overInfo=OVERDRIVE_INFO[S.overType],overReady=S.heat>=100&&S.over<=0;$("overGaugeLabel").textContent=`${overInfo.name.toUpperCase()} · ${S.over>0?"ACTIVE":Math.round(S.heat)+"%"}`;$("overdrive").classList.toggle("ready",overReady);$("overdrive").classList.toggle("active",S.over>0);$("overdrive").disabled=!overReady;$("overdrive").innerHTML=S.over>0?`${overInfo.name.toUpperCase()}<br><small>ACTIVE</small>`:overReady?`${overInfo.name.toUpperCase()}<br><small>READY</small>`:`${overInfo.name.toUpperCase()}<br><small>${Math.round(S.heat)}%</small>`;
 $("attack").textContent=S.over>0?"AUTO x3":"AUTO";$("dash").classList.toggle("ready",S.dashCd<=0);$("dash").classList.toggle("over",S.over>0&&S.overType==="pip"&&(S.pipSupport||0)>=2);$("dash").textContent=S.dashCd<=0?"DASH":"...";$("healthBar").style.width=(S.health/S.maxHealth*100)+"%";$("shieldPips").innerHTML=Array.from({length:S.maxShields},(_,i)=>`<i class="shieldpip ${i<S.shields?"":"empty"}"></i>`).join("");
 if(S.shields>=S.maxShields)$("regenText").textContent="FULL";else if(S.shieldRegenClock<0)$("regenText").textContent="WAIT "+Math.ceil(-S.shieldRegenClock)+"s";else $("regenText").textContent="REGEN "+Math.max(0,S.shieldRegenRate-S.shieldRegenClock).toFixed(1);
 const tgt=getAutoTarget();$("tip").textContent=S.bossActive?`${S.bossName} · ${Math.max(0,Math.round((enemies.find(e=>e.type==="boss")?.hp||0)))} HP`:S.waveState==="break"?`${S.stageEnding?"STAGE CLEAR":"REST"} ${Math.max(0,S.waveBreak).toFixed(1)}s`:!pipWithPlayer()?`PIP COLLECTING · BONUSES OFF`:tgt?`LOCK ${tgt.type.toUpperCase()} · ${S.waveKills}/${S.waveGoal} · PIP ♥ RANGE ${S.pipDetectRange}`:`STAGE ${S.stage} · ${Math.floor(S.stageTime)}s · ♥ RANGE ${S.pipDetectRange}`;
 if(S.pipHappy<=0&&S.run){const moods=S.over>0?(S.overType==="pip"?"✦ Pip: I'M WITH YOU — ALL THE WAY ✦":"✦ Pip: OVERDRIVE! ✦"):S.combo>=4?"✦ Pip: you're incredible":S.shields===0?"✦ Pip: I believe in you completely":S.health<50?"✦ Pip: stay with me, superstar":S.pipLevel>=5?"✦ Pip: being your partner is my favorite thing":S.pipLevel>=3?"✦ Pip: I'm so glad we're a team":"✦ Pip: you're doing amazing";$("pipMood").textContent=moods}
}
function finish(dead){
 if(S.end)return;S.end=true;S.run=false;const starEcho=bankStarEcho();let grade=S.score>=25000?"S":S.score>=16000?"A":S.score>=9000?"B":S.score>=4500?"C":"D";$("grade").textContent=grade;$("endTitle").textContent=dead?"Arena got you.":"Run complete.";$("endStats").innerHTML=[["Score",S.score.toLocaleString()],["Stage",S.stage],["Pip","Lv "+S.pipLevel],["Health",Math.ceil(S.health)]].map(x=>`<div class="mini"><span class="small">${x[0]}</span><b>${x[1]}</b></div>`).join("");const [playTitle,playLine]=styleTitle();let line=S.bestCombo>=6?"Pip noticed your rhythm immediately and is now unbearably proud of you. ":S.chains>=4?"Pip would like the record to show that your explosions were art. ":"Pip had the best time in the world surviving stages with you. ";$("endTitle").textContent=playTitle;$("endText").textContent=line+playLine+` Pip is now level ${S.pipLevel} (${pipBondName(S.pipLevel)}). You own ${S.audioUnlocks.size}/${AUDIO_CATALOG.length} sound unlocks. Pip remembers the XP and the music you earned together. You collected ${S.starsTotal} Wish Stars; ${starEcho} Star Echo${starEcho===1?"":"es"} will carry into the next run.`;$("end").classList.remove("hidden");burstTone(grade==="S"?440:220,5);
}
function drawGrid(t){
 const grid=42,ox=(((-CAM.x+W/2)%grid)+grid)%grid,oy=(((-CAM.y+H/2)%grid)+grid)%grid;X.strokeStyle=S.over>0?"#4f391324":"#18305235";X.lineWidth=1;X.beginPath();for(let x=ox;x<W;x+=grid){X.moveTo(x,0);X.lineTo(x,H)}for(let y=oy;y<H;y+=grid){X.moveTo(0,y);X.lineTo(W,y)}X.stroke();const big=grid*5,box=(((-CAM.x+W/2)%big)+big)%big,boy=(((-CAM.y+H/2)%big)+big)%big;X.strokeStyle=S.over>0?"#8a612518":"#244a761f";X.beginPath();for(let x=box;x<W;x+=big){X.moveTo(x,0);X.lineTo(x,H)}for(let y=boy;y<H;y+=big){X.moveTo(0,y);X.lineTo(W,y)}X.stroke();
}
